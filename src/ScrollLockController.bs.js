// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Belt_Id from "rescript/lib/es6/belt_Id.js";
import * as Belt_Set from "rescript/lib/es6/belt_Set.js";
import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as BodyScrollLock from "body-scroll-lock";

function lock(el) {
  BodyScrollLock.disableBodyScroll(el, {
        reserveScrollBarGap: true
      });
  
}

function unlock(el) {
  BodyScrollLock.enableBodyScroll(el);
  
}

function cmp(el1, el2) {
  if (el1 === el2) {
    return 0;
  } else {
    return 1;
  }
}

var DomElementsCmp = Belt_Id.MakeComparable({
      cmp: cmp
    });

function create(param) {
  var locksSet = {
    contents: Belt_Set.make(DomElementsCmp)
  };
  var lock = function (targetElement) {
    locksSet.contents = Belt_Set.add(locksSet.contents, targetElement);
    BodyScrollLock.enableBodyScroll(targetElement);
    
  };
  var unlock = function (targetElement) {
    locksSet.contents = Belt_Set.remove(locksSet.contents, targetElement);
    return function (param, param$1) {
      BodyScrollLock.disableBodyScroll(targetElement, param !== undefined ? Caml_option.valFromOption(param) : undefined);
      
    };
  };
  var clearLocks = function (prim) {
    BodyScrollLock.clearAllBodyScrollLocks();
    
  };
  var isEmpty = function (param) {
    return Belt_Set.isEmpty(locksSet.contents);
  };
  return {
          lock: lock,
          unlock: unlock,
          clearLocks: clearLocks,
          isEmpty: isEmpty
        };
}

export {
  lock ,
  unlock ,
  DomElementsCmp ,
  create ,
  
}
/* DomElementsCmp Not a pure module */
